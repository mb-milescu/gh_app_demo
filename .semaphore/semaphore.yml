version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
            - sem-version python 3.7
            - pip install virtualenv --user
            - venv test_cache
            - source test_cache/bin/activate
            - pip install --cache-dir .pip_cache -r requirements.txt
            - cache store
  - name: 'Block #2'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
            - sem-version python 3.7
            - pip install virtualenv --user
            - venv test_cache
            - source test_cache/bin/activate
            - cache restore
            - pylint __main__.py
